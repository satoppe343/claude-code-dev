{
  "name": "claude-code-dev",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/json": "^1.0.1",
    "eslint": "^10.0.2",
    "prettier": "^3.8.1"
  }
}
